steps:
  # In:
  #  user.id (str) - id of current user passed in from page
  # Out:
  #  flow.order_id (str) - id of selected order
  - type: component.order.list
    user_id: (@ user.id )
  - say: Here are the orders I found...
  - type: component.order.display
    orders: (@ flow.result )
  - say: selected (@ flow.result )
